---
extends: default

rules:
  document-start:
    present: true
  indentation:
    spaces: 2
  line-length:
    max: 80
  empty-lines:
    max: 1
  truthy:
    ignore: "on.*"  # Suppress truthy warnings for GitHub Actions 'on' fields
